// eslint-disable-next-line no-shadow
export enum GameStatus {
  INITIAL_STATE,
  POST_INTRO,
  ALL_GAMES_COMPLETE,
  POST_OUTRO,
}

// eslint-disable-next-line no-shadow
export enum IslandStatus {
  NOT_DISCOVERED,
  OPENED,
  DISCOVERED,
  COMPLETE,
}
